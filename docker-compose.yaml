services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: angular-best-doc-web
    volumes:
      - ".:/app"
    env_file:
      - .env
    ports:
      - "81:80"
    tty: true
